// Individual export of elements from a module one by one
// AKA named exports
// req: identifier and body or block
export let apiKey = "Some api key generated randomly";
export let elemAvaivableOutSideThisCurrentFile = 34556;

// Only a single default export per module
// It is the at least elem viewable outside from the module per default;
// and it does not req an identifer while exporting it, though it will require to be 
// identified in the module or target from wheer it has or will be imported
export default "main or can be anything or any type of thing the language can understand, var, func, obje etc";