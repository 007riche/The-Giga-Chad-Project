// Served as domain.com/news
// served either by the file name or the folder name 
// having an index.js file
// In the current case, our news.js current file.

// Note: the index.js files will serve the endpoint root path

function NewsPage() {
    return <h1>The News Page</h1>
}

export default NewsPage;