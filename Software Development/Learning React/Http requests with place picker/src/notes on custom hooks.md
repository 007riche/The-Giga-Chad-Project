## Custom hooks
- Hooks are callable from inside Component functions or other hook function
- Hooks should be called on top level, inside their caller
In the end, hooks are just functions
React tries to look form function starting with "use" to enforce hooks either custom or built-in hooks' rules on them